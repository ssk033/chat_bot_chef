global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: "meal-it"
    metrics_path: "/api/metrics"
    static_configs:
      - targets:
          - app:3000